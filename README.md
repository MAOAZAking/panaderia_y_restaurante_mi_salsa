# panaderia_y_restaurante_mi_salsa


# Ultimo pront para la mejora
Necesito que me ayudes a mejorar este codigo haciendo que si el nombre del producto contiene la palabra bandeja o abreviatura de esta el valor sea igual a 14000, pero si contiene la palabra almuerzo o la abreviatura de este otro el valor sea igual a 16000, tambien necesito que este archvio analize le folder a ver si encuentra el archivo "cuenta_por_cobrar.xlxs" el cual tiene en la celda A1 Nombre y en la B2 tota, debemos agregar otro metodo de pago que es anotar, a esta factura se le extrae el nombre, y el total y agregarlo a este excel que te dije, pero en la factura debe quedar estipulado como "Efectivo" porque una factura electronica no puede tener este metodo de pago sino efectivo pero lo que desencadena es la agregacion de esta factura en el archivo de excel, si este archvio no existe lo crea y agrega los titulos correspondientes en la respectiva columna como te dije; pero tambien necesito que cada factura quede registrada en un excel llamado "facturas.xlxs" donde cada hoja va a tener el nombre del mes seguido de un guion seguido del numero del año, ejepmlo actualmente (septiembre-2026) y debe tener las columnas "NOMBRE", "TOTAL", "FECHA", estas las debe crear y la forma de determinar si ya exuste es que como este valor debe estar en la   fila 1 osea que si la celda A1 esta llena ya existe, y asi debe detectar la ultima fila, y en la siguiente se añade la factura actual, y la forma en como hara el cambio de mes es que debe leer la fecha actual del dispitivo y debe entrar en la utlima hoj,a creada, como se crean en orden y no se ordenan por nombre, podemos usar el filtro de entrar en la ultima hoja, leer el ultimo registro en la columna C donde tenemos la fecha y si el mes de la fecha de este es igual al mes actual, entonces abajo de este ultimo registro guarda los datos de la factura actual, pero si es diferente mes el hoy al ultimo registro, captura el mes actual y crea la nueva hoja con el nombre correpsondiente, pone los titulos base que te dije de nombre, total y fecha y pone el primer registro de ese mes; tambien ayudame a solucionar el error de que cuando a un nombre del cliente se le coloca el nit se almacene completo como registro porque veo que sigamos ejemplo "empresa de papas nit 1234567" cuando vuelvo a poner el nombre "empresa de" aparece el resultado: "emopresa de papas" pero sin el nit y debe guardar esto, porque tambien es un dato importante y es muy dificil poner de nuevo numero a numero, y tambien has compatible el codigo si se pone la palabra cc en el nombre, y el valor despues de cc debe ponerse en la factura donde dice nit/cc porque actualmente solo funciona con nit y no con cc